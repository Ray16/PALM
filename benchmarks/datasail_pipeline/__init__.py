"""DataSAIL config-driven splitting pipeline + web app — a benchmark baseline (the core methods live in PALM.splitters).

Run with:  python -m PALM.benchmarks.datasail_pipeline <config.yaml>
(was:      python -m PALM <config.yaml>)
"""
